#########com.yt.crm.biz.visitaction.enums.VisitActionTaskStatusEnum
package com.yt.crm.biz.visitaction.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum VisitActionTaskStatusEnum {

    VALID(0, "生效中"),

    INVALID(1, "已失效");

    private final Integer code;
    private final String desc;
}

#########com.yt.crm.biz.visitaction.enums.VisitActionTaskContentStatusEnum
package com.yt.crm.biz.visitaction.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum VisitActionTaskContentStatusEnum {

    VALID(0, "生效中"),

    INVALID(1, "已失效");

    private final Integer code;
    private final String desc;
}

#########com.yt.crm.biz.visitaction.enums.VisitActionTaskTypeEnum
package com.yt.crm.biz.visitaction.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum VisitActionTaskTypeEnum {

    BEFORE_SALE(0, "卖进前"),

    AFTER_SALE(1, "卖进后");

    private final Integer code;
    private final String desc;
}

#########com.yt.crm.biz.visitaction.enums.VisitActionTaskTeamTypeEnum
package com.yt.crm.biz.visitaction.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum VisitActionTaskTeamTypeEnum {

    BD(0, "BD团队");

    private final Integer code;
    private final String desc;
}

#########com.yt.crm.biz.visitaction.enums.VisitActionTaskContentTypeEnum
package com.yt.crm.biz.visitaction.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum VisitActionTaskContentTypeEnum {

    MANUAL(0, "人工设定"),

    STRATEGY(1, "系统策略");

    private final Integer code;
    private final String desc;
}

########com.yt.crm.biz.visitaction.enums.VisitActionTaskStrategyEnum
package com.yt.crm.biz.visitaction.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum VisitActionTaskStrategyEnum {

    BUY_INVENTORY(1, "必拍+库存策略（仅限卖进后）");

    private final Integer code;
    private final String desc;
}


#########com.yangt.cif.visitrecord.domain.enums.RecordTypeEnum
package com.yangt.cif.visitrecord.domain.enums;

public enum RecordTypeEnum {

    HSP_MGR_VISIT_SHOP(1, "服务商经理拜访门店"),
    HSP_MGR_VISIT(2, "服务商经理拜访服务商"),
    HIPAC_SHOP_BD(3, "BD门店拜访"),
    HIPAC_SHOP_ONLINE(4, "电销门店拜访"),
    HSP_VISIT_SHOP(5, "服务商业务员拜访门店"),
    HSP_MGR_VISIT_V2(6, "服务商经理拜访门店小记V2版本");

    private final Integer code;

    private final String desc;

    RecordTypeEnum(Integer code, String desc) {
        this.code = code;
        this.desc = desc;
    }

    public Integer getCode() {
        return code;
    }

    public String getDesc() {
        return desc;
    }
}

########com.yangt.cif.visitrecord.domain.enums.OffLineVisitModeEnum
package com.yangt.cif.visitrecord.domain.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum OffLineVisitModeEnum {

    UNKNOWN(0, "未知"),
    VALID_VISIT(1, "有效拜访"),
    NORMAL_VISIT(2, "一般拜访");

    private final Integer code;

    private final String desc;
}

########com.yangt.cif.visitrecord.domain.enums.OnLineVisitModeEnum
package com.yangt.cif.visitrecord.domain.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum OnLineVisitModeEnum {

    UNKNOWN(0, "未知"),
    VALID_VISIT(1, "有效拜访"),
    INVALID_VISIT(2, "无效拜访");

    private Integer code;

    private String desc;
}

########com.yangt.cif.visitrecord.domain.enums.OffLineVisitTypeEnum
package com.yangt.cif.visitrecord.domain.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum OffLineVisitTypeEnum {

    UNKNOWN(0, "未知"),
    LOCAL_VISIT(1, "上门拜访"),
    REMOTE_VISIT(2, "远程拜访");

    private Integer code;

    private String desc;
}

########com.yangt.cif.visitrecord.domain.enums.OnLineVisitTypeEnum
package com.yangt.cif.visitrecord.domain.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum OnLineVisitTypeEnum {

    UNKNOWN(0, "未知", false),
    PHONE(1, "电话", true),
    WX(2, "微信文字", false),
    WEI_XIN_CALL(3, "微信语音", true),
    QW_TEXT(4, "企微文字", false),
    QW_VOICE(5, "企微电话", true);

    private Integer code;

    private String desc;

    private Boolean hasVoice;
}

#########com.crm.app.api.visit.enums.VisitAimsTypeEnum
package com.crm.app.api.visit.enums;

import lombok.AllArgsConstructor;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.lang.StringUtils;

import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.stream.Collectors;

@AllArgsConstructor
public enum VisitAimsTypeEnum {

    CUSTOMER_FEELING(1, "维护客情", Arrays.asList("拜访计划", "门店管理列表", "门店详情", "地图"), 0, VisitAimsTypeScopeEnum.ALL, null),
    NEW_GOODS_ENTER_SHOP(2, "新品进店", Collections.singletonList("B类加盟工单"), 1, VisitAimsTypeScopeEnum.ALL, null),
    PRODUCT_ACTIVITY(3, "查看动销", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),
    TRAIN(4, "产品培训", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),
    MARKET_HANDOVER(5, "市场交接", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),
    CUSTOMER_COMPLAINT(6, "处理客诉", Arrays.asList("B类清退工单", "窜货工单"), 1, VisitAimsTypeScopeEnum.ALL, null),
    SHOP_ACTIVITY(7, "场外活动", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),
    NEW_CUSTOMER(8, "新客开发", Collections.singletonList("商机管理"), 1, VisitAimsTypeScopeEnum.ALL, null),
    WITH_VISIT(9, "陪访", Collections.emptyList(), 0, VisitAimsTypeScopeEnum.ALL, null),
    PROJECT_ZERO_ACTION(10, "P0动作", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),
    NET_ACTION(11, "网维宣导", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),
    CORE_SHOP_VISIT(12, "核心门店拜访", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_SP,
            StringUtils.join(new String[]{"1、当月进货件数：", "2、给门店的进货政策：", "3、门店现有客户数：",
                    "4、当月门店销售目标：", "5、门店销售方案：", "6、本次推进事项："}, "\\r\\n")),
    MILK_POWDER_ACTION(13, "奶粉活动", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL,
            StringUtils.join(new String[]{"活动类型：", "邀约人数：", "到场人数：", "销售听数：", "开新客人数：",
                    "礼品类型：", "礼品发放数量："}, "\\r\\n")),

    TONGXIANG_RESPOSITORY_CHECK(14, "童享库存盘点", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_SP,
            StringUtils.join(new String[]{"1段库存：", "2段库存：", "3段库存：", "其他库存："}, "\\r\\n")),

    TONGXIANG_NEW_CUSTOMER_ACTIVITY(15, "童享新客活动", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_SP, null),

    IMPORTANT_SP(16, "重点服务商", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_SP, null),

    SNACK_STORE_INVENTORY(17, "零辅食库存盘点", Collections.emptyList(), 0, VisitAimsTypeScopeEnum.ALL,
            StringUtils.join(new String[]{"1、喵小侠店内库存数量：", "2、喵小侠店内效期过半产品数量：", "3、喵小侠店内效期过半产品说明：",
                    "4、禧聪店内库存数量：", "5、禧聪店内效期过半产品数量：", "6、禧聪店内效期过半产品说明："}, "\\n")),

    YQBF(18, "疫情拜访", Collections.emptyList(), 0, VisitAimsTypeScopeEnum.ALL, null),


    OTHER(0, "其他", Arrays.asList("选择门店", "到店神器工单", "到店神器活动榜单"), 1, VisitAimsTypeScopeEnum.ALL, null),

    VIP88(19, "88VIP", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),

    VIP188(20, "188VIP", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),

    A_BIG_SHOP_POLICY(21, "A类大门店政策", Collections.emptyList(), 0, VisitAimsTypeScopeEnum.ONLY_BD, null),

    VIP688(22, "688VIP", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ALL, null),

    DANONE_VISIT(23, "达能专访", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_BD, null),

    YING_PEI_FEN(24, "婴配粉", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_BD, null),

    A2_CUSTOM(25, "a2专访", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_BD, null),

    BEIYUE_VISIT(26, "贝悦专访", Collections.emptyList(), 1, VisitAimsTypeScopeEnum.ONLY_BD, null);

    private final Integer type;
    private final String name;
    private final List<String> page;
    private final Integer status;
    private final VisitAimsTypeScopeEnum scope;
    private final String content;

}

########com.yangt.cif.visitrecord.domain.enums.RecordVisitStatusEnum
package com.yangt.cif.visitrecord.domain.enums;

public enum RecordVisitStatusEnum {

    UNKNOWN(0, "未知"),
    UN_FINISH(1, "未完成"),
    FINISH(2, "已完成");

    private final Integer code;

    private final String desc;

    RecordVisitStatusEnum(Integer code, String desc) {
        this.code = code;
        this.desc = desc;
    }
}

########com.yangt.cif.visitrecord.domain.enums.RecordVerifyStatusEnum
package com.yangt.cif.visitrecord.domain.enums;

import lombok.AllArgsConstructor;

@AllArgsConstructor
public enum RecordVerifyStatusEnum {

    UNKNOWN(0, "未知"),
    INVALID(1, "不合格"),
    VALID(2, "合格");

    private Integer code;

    private String desc;
}